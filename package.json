{
  "name": "santacruz-theater",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "netlify": "netlify dev",
    "merge-remote-dev": "git pull --rebase origin dev",
    "build": "tsx scripts/check-json.ts && tsx scripts/check-shows.ts && tsx scripts/check-activities.ts && astro build",
    "preview": "tsx scripts/check-json.ts && tsx scripts/check-shows.ts && tsx scripts/check-activities.ts && astro preview",
    "release": "git diff --quiet && git diff --cached --quiet && git checkout dev && git pull origin dev && git checkout main && git pull origin main && git merge dev && git push origin main && git checkout dev && git merge main && git push origin dev",
    "check-json": "tsx scripts/check-json.ts",
    "check-shows": "tsx scripts/check-shows.ts",
    "check-activities": "tsx scripts/check-activities.ts"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0",
    "@netlify/functions": "^5.3.0",
    "astro": "^6.3.7",
    "google-auth-library": "^11.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/node": "^25.9.1",
    "netlify-cli": "^26.1.0",
    "tsx": "^4.19.4",
    "typescript": "^6.0.3"
  }
}
