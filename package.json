{
  "name": "santacruz-theater",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "tsx scripts/check-shows.ts && tsx scripts/check-auditions.ts && astro build",
    "preview": "tsx scripts/check-shows.ts && tsx scripts/check-auditions.ts &&  astro preview",
    "release": "git diff --quiet && git diff --cached --quiet && git checkout dev && git pull origin dev && git checkout main && git pull origin main && git merge dev && git push origin main && git checkout dev && git merge main && git push origin dev",
    "check-shows": "tsx scripts/check-shows.ts",
    "check-auditions": "tsx scripts/check-auditions.ts"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0",
    "@netlify/functions": "^5.3.0",
    "astro": "^6.3.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/node": "^25.9.1",
    "tsx": "^4.19.4",
    "typescript": "^6.0.3"
  }
}
