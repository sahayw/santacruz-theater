---
import BaseHead from '../components/BaseHead.astro'
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title="Santa Cruz Theater Hub"
      description="Live theater performances, companies, and auditions across Santa Cruz County."
      canonicalPath="/"
    />
    <!-- Netlify Identity widget: handles invite/recovery tokens in URL fragment -->
    <script src="https://unpkg.com/netlify-identity-widget@1/build/netlify-identity-widget.js"
    ></script>
    <script>
      if ((window as any).netlifyIdentity) {
        ;(window as any).netlifyIdentity.on('login', () => {
          window.location.href = '/admin/'
        })
      }
    </script>
  </head>
  <body>
    <div class="page">
      <div class="page-bg"></div>
      <div class="page-overlay"></div>

      <div class="page-content">
        <header class="hero-text">
          <p class="hero-eyebrow">Live theater in</p>
          <h1 class="hero-title">Santa Cruz</h1>
          <p class="hero-tagline">Performances across the county's leading companies</p>
        </header>

        <div class="hero-cards-gap"></div>

        <div class="cards-grid">
          <a href="/calendar" class="section-card card--wide">
            <div class="card-accent"></div>
            <div class="card-icon">
              <svg
                width="26"
                height="26"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.5"
                stroke-linecap="round"
                stroke-linejoin="round"
                aria-hidden="true"
              >
                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                <line x1="16" y1="2" x2="16" y2="6"></line>
                <line x1="8" y1="2" x2="8" y2="6"></line>
                <line x1="3" y1="10" x2="21" y2="10"></line>
              </svg>
            </div>
            <div class="card-main">
              <h2 class="card-title">Calendar</h2>
              <p class="card-desc">
                View performances over coming months, get details of individual shows, and purchase
                tickets (if available online).
              </p>
            </div>
            <span class="card-cta">Explore &rarr;</span>
          </a>

          <a href="/companies" class="section-card card--sub">
            <div class="card-accent"></div>
            <div class="card-icon">
              <svg
                width="26"
                height="26"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.5"
                stroke-linecap="round"
                stroke-linejoin="round"
                aria-hidden="true"
              >
                <rect x="2" y="7" width="20" height="14" rx="2"></rect>
                <path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"></path>
              </svg>
            </div>
            <h2 class="card-title">Companies</h2>
            <p class="card-desc">Profiles of theater companies in Santa Cruz County.</p>
            <span class="card-cta">Explore &rarr;</span>
          </a>

          <div class="section-card card--sub card--auditions">
            <div class="card-accent"></div>
            <div class="card-icon">
              <svg
                width="26"
                height="26"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.5"
                stroke-linecap="round"
                stroke-linejoin="round"
                aria-hidden="true"
              >
                <path d="M2 9a5 5 0 0 1 10 0v4a5 5 0 0 1-10 0V9z"></path>
                <path d="M5.5 12.5c.5.4 1.5.4 2 0"></path>
                <path d="M9 10c-.5-.7-1.5-.7-2 0"></path>
                <path d="M12 13a5 5 0 0 1 10 0V9a5 5 0 0 1-10 0"></path>
                <path d="M16.5 15.5c.5-.4 1.5-.4 2 0"></path>
                <path d="M19 12c-.5.7-1.5.7-2 0"></path>
              </svg>
            </div>
            <h2 class="card-title">Auditions & Events</h2>
            <p class="card-desc">Upcoming audition calls and other theater events.</p>
            <div class="card-links">
              <a href="/events" class="card-cta card-explore">Explore &rarr;</a>
              <a href="/subscribe" class="card-subscribe">Subscribe &rarr;</a>
            </div>
          </div>

          <a href="/services" class="section-card card--sub">
            <div class="card-accent"></div>
            <div class="card-icon">
              <svg
                width="26"
                height="26"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.5"
                stroke-linecap="round"
                stroke-linejoin="round"
                aria-hidden="true"
              >
                <circle cx="12" cy="12" r="3"></circle>
                <path
                  d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"
                ></path>
              </svg>
            </div>
            <h2 class="card-title">Services</h2>
            <p class="card-desc">
              Connect with the people and services you need to support your production.
            </p>
            <span class="card-cta">Explore &rarr;</span>
          </a>
        </div>

        <div class="page-footer">
          <a href="/about" class="about-link">About this site</a>
          <p class="photo-credit">
            Photo: <a href="https://unsplash.com" target="_blank" rel="noopener">Unsplash</a>
          </p>
        </div>
      </div>
    </div>
  </body>
</html>

<style>
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  body {
    font-family: 'DM Sans', sans-serif;
    background: #120c06;
    color: #1a1612;
    min-height: 100vh;
  }

  /* ── FULL-PAGE WRAPPER ── */
  .page {
    position: relative;
    min-height: calc(100vh - 44px);
    margin-top: 44px;
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }

  .page-bg {
    position: absolute;
    inset: 0;
    background-image: url('/images/home/hero.jpg');
    background-size: cover;
    background-position: center top;
  }

  /* Gradient: readable title at top, visible curtain mid, darker at bottom for cards */
  .page-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      rgba(18, 12, 6, 0.64) 0%,
      rgba(18, 12, 6, 0.22) 38%,
      rgba(18, 12, 6, 0.8) 100%
    );
  }

  /* ── CONTENT COLUMN ── */
  .page-content {
    position: relative;
    z-index: 2;
    flex: 1;
    display: flex;
    flex-direction: column;
    padding: 20px 32px;
    gap: 0;
    max-width: 960px;
    width: 100%;
    margin: 0 auto;
  }

  /* Flex spacers: top gets 3 parts, middle gets 2, bottom gets 1 */
  .page-content::before,
  .page-content::after {
    content: '';
  }

  .page-content::before {
    flex: 3;
  }
  .page-content::after {
    flex: 1;
  }

  .hero-cards-gap {
    flex: 2;
    min-height: 16px;
  }

  /* ── HERO TEXT ── */
  .hero-text {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    /* min so very short viewports don't squash to zero */
    min-height: 120px;
  }

  .hero-eyebrow {
    font-size: 14px;
    font-weight: 400;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: #c8a96e;
    margin-bottom: 10px;
  }

  .hero-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(44px, 7vw, 80px);
    font-weight: 700;
    color: #fff;
    line-height: 1;
    letter-spacing: -0.01em;
    margin-bottom: 12px;
  }

  .hero-tagline {
    font-size: 15px;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.7);
    letter-spacing: 0.02em;
  }

  /* ── SECTION CARDS ── */
  .cards-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
    flex-shrink: 0;
    padding: 0 8px 8px;
  }

  .section-card {
    background: #fff;
    border-radius: 8px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    text-decoration: none;
    color: #1a1612;
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.3);
    transition:
      box-shadow 0.2s,
      transform 0.2s;
    -webkit-tap-highlight-color: transparent;
  }

  .section-card:hover {
    box-shadow: 0 8px 40px rgba(0, 0, 0, 0.42);
    transform: translateY(-2px);
  }

  .section-card:active {
    transform: translateY(0);
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.28);
  }

  .card-accent {
    height: 3px;
    background: #c8a96e;
    flex-shrink: 0;
  }

  .card--soon .card-accent {
    background: #d4ccc2;
  }

  .card-icon {
    padding: 16px 20px 0;
    color: #c8a96e;
    flex-shrink: 0;
  }

  .card--soon .card-icon {
    color: #b5ada4;
  }

  .card-title {
    font-family: 'Playfair Display', serif;
    font-size: 19px;
    font-weight: 600;
    padding: 8px 20px 0;
    line-height: 1.2;
    color: #1a1612;
    flex-shrink: 0;
  }

  .card--soon .card-title {
    color: #6b6259;
  }

  .card--sub .card-title {
    font-size: 15px;
  }

  .card-desc {
    font-size: 13px;
    font-weight: 400;
    color: #6b6259;
    padding: 6px 20px 0;
    line-height: 1.58;
    flex: 1;
  }

  .card--sub .card-desc {
    font-size: 12px;
  }

  .card-cta {
    display: block;
    padding: 12px 20px 16px;
    font-size: 12px;
    font-weight: 500;
    color: #c8a96e;
    letter-spacing: 0.02em;
    text-decoration: none;
    flex-shrink: 0;
  }

  /* ── AUDITIONS CARD — two-link layout ── */
  .card--auditions {
    position: relative;
    cursor: pointer;
  }

  /* Stretched link: clicking anywhere on the card body goes to /auditions */
  .card--auditions .card-explore::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .card-links {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 10px 20px 16px;
  }

  /* card-cta padding override inside card-links */
  .card-links .card-cta {
    padding: 0;
  }

  .card-subscribe {
    position: relative;
    z-index: 1;
    font-size: 12px;
    font-weight: 500;
    color: #c8a96e;
    text-decoration: none;
    letter-spacing: 0.02em;
    margin-left: auto;
    flex-shrink: 0;
  }

  .card-soon-badge {
    display: inline-block;
    margin: 10px 20px 16px;
    padding: 3px 10px;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #9b9289;
    background: #f5f2ee;
    border-radius: 20px;
    border: 1px solid #e0d9d0;
    align-self: flex-start;
    flex-shrink: 0;
  }

  /* ── WIDE CALENDAR CARD ── */
  .card--wide:hover {
    transform: none;
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.3);
  }

  .card--wide {
    grid-column: 1 / -1;
    flex-direction: row;
    align-items: center;
  }

  .card--wide .card-accent {
    width: 3px;
    height: auto;
    align-self: stretch;
  }

  .card--wide .card-icon {
    padding: 0 8px 0 16px;
    align-self: center;
  }

  .card--wide .card-main {
    flex: 1;
    padding: 16px 12px;
    min-width: 0;
  }

  .card--wide .card-title {
    padding: 0;
    margin-bottom: 4px;
  }

  .card--wide .card-desc {
    padding: 0;
    flex: unset;
  }

  .card--wide .card-cta {
    padding: 0 20px;
    white-space: nowrap;
  }

  /* ── PAGE FOOTER (about + photo credit) ── */
  .page-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 8px 4px;
  }

  .about-link {
    font-size: 12px;
    color: rgba(255, 255, 255, 0.8);
    text-decoration: none;
    letter-spacing: 0.03em;
    padding: 2px 6px;
    border-radius: 3px;
    transition: background 0.15s;
  }

  .about-link:hover {
    background: rgba(255, 255, 255, 0.15);
  }

  /* ── PHOTO CREDIT ── */
  .photo-credit {
    font-size: 10px;
    color: rgba(255, 255, 255, 0.5);
  }

  .photo-credit a {
    color: rgba(255, 255, 255, 0.5);
    text-decoration: none;
  }

  /* ── TABLET (portrait and landscape) ── */
  @media (max-width: 860px) {
    .page-content {
      padding: 16px 24px;
    }
  }

  /* ── PHONE ── */
  @media (max-width: 600px) {
    .page-content {
      padding: 16px 16px 20px;
      max-width: 440px;
    }

    /* Collapse flex spacers on phone — use fixed gaps instead */
    .page-content::before,
    .page-content::after {
      flex: 0;
    }

    .hero-cards-gap {
      flex: 0;
      min-height: 32px;
    }

    .hero-text {
      min-height: 90px;
    }

    .cards-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .card--wide {
      flex-direction: column;
      align-items: stretch;
    }

    .card--wide .card-accent {
      width: auto;
      height: 3px;
    }

    .card--wide .card-icon {
      padding: 16px 20px 0;
      align-self: auto;
    }

    .card--wide .card-main {
      padding: 8px 20px 0;
    }

    .card--wide .card-cta {
      padding: 12px 20px 16px;
    }

    .card-desc {
      /* Shorten to 2 lines on phone to keep cards compact */
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
  }
</style>
